# Scope: Gate C2 — Wave-3 Stability Audit (weighting latency, multi-user prime size/latency)

Status: APPROVED (peer-reviewed 2026-07-15, APPROVE-WITH-CORRECTIONS — 3 HIGH pass-bar fixes applied: A1 sweeps TOTAL node count not just roster [get_nodes_by_type scans the whole graph — roster-only sweep passes trivially]; no cross-mode latency comparison [multi-project timing carries dispatch overhead single-project lacks]; A2's bar made absolute + like-for-like [pre-TC16 baseline can't gate post-TC16 prime]). Gate task 85b2b4de8aee (stays in_progress through C2); epic cedf4a8457e9. Battle plan :62: "C2 after Wave 3 — search latency with weighting, multi-user prime size/latency (weighting doesn't exist until TC9, so C1 can't test it)." Authorship split per C1 precedent (doc:d404a39edcca): any measurement harness is an implementer WP Vorpid builds under this brief; Vince RUNS it and gates the results (running a script ≠ writing one). Remediation clause: a C2 failure blocks the epic-close gates — fix WP or explicit Colton-approved acceptance before Gate B-final/D.

## WHAT C2 MUST ANSWER (from the battle plan, sharpened into measurables)

1. **Search latency with weighting.** TC9 added per-hit work (node fetch was already TC10's; TC9 adds one person-map build per call + one dict lookup + multiply per hit + a sort of the uncapped round list). Question: does weighted search stay within budget at production scale?
2. **Multi-user prime size + latency.** TC16 adds role derivation (one person scan) + rollup/manager-decision sections. Question: does prime stay fast and BOUNDED in output size on a graph with many users/roles — the multi-tier team this epic exists for?
3. **Regression sweep**: nothing Wave 2/3 added slows journal replay or startup (all query-time features) — re-run the C1 replay benchmark at the C2 pin and compare.

## PART A — VINCE'S RUN CHECKLIST (no code authored by Vince)

- A1 (peer-review corrected — the REAL cost driver): `_person_seniority_map` calls `get_nodes_by_type(PERSON)`, which scans EVERY graph node (storage.py:435-449) — per-search cost is O(total_nodes), NOT O(roster). The sweep therefore varies BOTH axes: total node count 1k / 5k / 10k × roster 0 / 10 / 100. Record weighted-search p50/p95 over ≥20 reps at limit 10 and limit 50. PASS: weighting overhead (roster-N vs roster-0 delta AT THE SAME node count) ≤ 10ms p95; overhead growth across node counts stays LINEAR-in-nodes (the built-in scan cost, disclosed) with no super-linear signature. NO cross-mode comparison: single-project timings (direct `_search_cognition`, injected fixed-vector embedder) and project="*" timings (full ctx/_MockMcp dispatch — the fan-out has no standalone core, so dispatch overhead is unavoidable there) are judged each against their OWN roster-0 control, never against each other (apples-to-oranges, peer-review flagged).
- A2 (peer-review corrected — no cross-era bar): TWO measurements. (i) Like-for-like regression: re-run C1's EXACT B3 prime scenario (1k-node/10-person fixture, no roles) at the C2 pin — PASS: within noise of episode 3a1d1096b57a's recorded ~11ms medians. (ii) New-topology absolute budget: graphs with 1 / 5 / 25 distinct stamped users, 3-level reports_to chain, one manager with 10 reports holding in_progress/blocked/stale tasks — PASS: prime p95 ≤ 250ms absolute at harness scale AND output size respects every section cap (rollup never exceeds prime_rollup_cap=5 rows regardless of report count — the caps are the size control per the TC16 brief doc:01acff533612; verify they actually bound the multi-user case). No 2×-of-pre-TC16 comparisons — a role-less baseline cannot gate role-bearing output.
- A3: Re-run bench_wave1.py --journal-dir on the REAL journal at the C2 pin (replay + convergence numbers) and compare against the C1-recorded figures (episode 3a1d1096b57a) — replay time within noise of C1's, node/edge counts consistent with growth since.
- A4: Byte-size sanity on the real graph: generate prime as the real owner email at the pin; confirm role sections render (Colton is a manager iff anyone reports_to him in the roster — likely no roster yet → sections absent is the EXPECTED result and itself a pass of the role-less compat invariant on real data).
- A5: chromadb flake watch: note any cross-process flakes seen during Wave 2/3 gates (one known: test_chromadb_cross_process concurrent_open at TC9 gate, passed in isolation) — C2 records the tally; escalate to a fix WP only if the same test flaked ≥3 times across the epic's gates.
- Verdict recorded as a graph episode with the numbers; Gate C task 85b2b4de8aee closes only after C2 PASS.

## PART B — HARNESS SPEC (Vorpid WP, small)

Extend scripts/bench_wave1.py (or sibling scripts/bench_wave3.py if cleaner) — same conventions: stdlib + repo imports only, human-readable output + JSON tail, --journal-dir mode reuse:
- Synthetic fixture generator gains: --persons N (registered person nodes, seniority round-robin across the 4 tiers), stamped recorded_by across generated nodes (distinct emails matching the roster ± unregistered extras), from_agent mix (~30% true), a task population with claimed_by/transitions (for prime rollup timing) including stale-aged claims (timestamp backdating, NOT sleeps).
- Search benchmark mode: times cognition_search core (_search_cognition with a mocked/fixed embedder, same _FixedGen pattern as the TC9 tests — model load time must NOT pollute the search timing) over R repetitions × configurable limit; reports p50/p95 and the person-scan count (assert the memo: scans == calls). Scan counting: monkeypatch `cognition_tools._person_seniority_map` with a counting wrapper restored in `finally` (the _WarningCapture pattern, bench_wave1.py:491-503) — flagged honestly: private-attribute patching, a fragility step up from the injected-embedder seam; acceptable in a scripts/ measuring device, never in tests.
- Fixture-scale knob: --nodes N so A1's total-node-count sweep (1k/5k/10k) is drivable; node generation stays seeded/deterministic at every scale.
- Prime benchmark mode: times generate_prime for a given --email over R repetitions; reports p50/p95 wall-clock + output byte size + per-section row counts.
- Multi-project search mode: two temp graphs, fan-out timing.
- NO product code changes; NO new deps; NOT part of the plugin surface (scripts/ only, like bench_wave1) — no version bump, merge without release (C1-harness precedent, ed5d53a).

## ACCEPTANCE (for the harness WP)

Deterministic (seeded) fixture; runs green on a clean worktree via uv run; JSON tail parseable; README-in-docstring usage examples; ruff clean; no src/ diff; journal untouched on branch. Gate: standard isolated-worktree pytest+ruff (harness has no tests requirement beyond import-cleanliness — it IS the measuring device; C1 precedent).

## SEQUENCING

Brief peer-reviewed now; harness WP handed to Vorpid AFTER TC16 gates (C2 measures the post-TC16 pin — building it against pre-TC16 prime would miss the role sections; the search-benchmark half could build earlier, but with ONE implementer and zero idle windows in the pipeline, splitting the WP would just pull Vorpid off TC16 without saving critical-path time). Vince runs Part A at the TC16-merged pin. C2 verdict before Gate B-final and Gate D. Remediation-clause note (flagged for Colton): the battle plan's generic "C failure blocks the next wave" has no literal next wave after Wave 3 — this brief substitutes "blocks the epic-close gates (B-final/D)"; recorded for Colton's async confirmation, not silently assumed.

## KNOWN LIMITS

Harness-scale numbers, not production telemetry — budgets are sanity rails against algorithmic blowups (the O(N×P) class), not SLAs. Timing on a dev machine is noisy: p95 over ≥20 reps + comparing DELTAS (roster-0 vs roster-100) controls for that, same discipline as C1's B3 noise handling (judged across runs, not single-run booleans).
