# WP Brief — Train A: Dashboard (Board epics + tab title + People view + cache headers)

Author: Vince. For: Vorpid. Epic: cedf4a8457e9 (team-cognition). Tasks: e984f2c7c65a, 99fa0d4da1ad, 5d4e2bd60d17, rider 3f43360014ee.
Branch: one WP branch for the train (suggest `wp-dashv3`). Read-only dashboard doctrine applies throughout — NO write/edit affordances anywhere in this train (Colton's standing view-only ruling, decision c0b7b559e3a7 area).

## A1 — Board view: column (kanban) ONLY, split by epics (task e984f2c7c65a, Colton's ruling, do not relitigate)

1. REMOVE the Tree-view toggle and all board-tree markup/logic entirely. Colton observed the toggle rendering the graph constellation instead of a task tree — note the root cause in the WP notes (one paragraph), but it is being REMOVED, not fixed.
2. Within each status column, group tasks under their TOP-LEVEL ANCESTOR epic: epic header (epic summary, linkable to its drawer) + that epic's tasks beneath it. Tasks with no epic ancestor go under a trailing "(no epic)" group at the bottom of each column.
3. "Top-level ancestor" = walk parent_id to the root task. api.py already exposes parent_id; traversal client-side or a small API addition — your choice, note it in the WP notes.
4. Keep existing Board behaviors that aren't the tree toggle (cancelled toggle, drawers, transition timeline, provenance chips).

Degenerate case, pinned: an epic task, being its own top-level ancestor, renders ONLY as a group header — never duplicated as a child card beneath itself, and never as a standalone card elsewhere on the Board. The header carries the epic's own status/priority as a chip; the header appears in each status column that contains at least one of its descendant tasks.

Acceptance: Board renders columns only (no toggle in the DOM); every non-epic task appears exactly once, under the correct epic header in its status column; epics appear only as headers per the pinned rule; "(no epic)" group present and trailing; epic ordering within a column is stable (suggest: by epic priority then recency — note your choice).

## A2 — Browser tab title includes the project folder name (task 99fa0d4da1ad, Colton-dictated)

Title must lead with the project folder name (basename of the served repo root) so it survives tab-width truncation — e.g. `vibe-cognition — Vibe Cognition`. It must be correct ON INITIAL PAGE LOAD (server-side injection into the served index.html, or equivalent), not only after a JS data fetch. Note: index.html is served via FileResponse from the static dir (dashboard/server.py `_resolve_static_dir`) — a tiny render-on-read substitution is acceptable; keep /static mounts untouched.

COUPLING WITH A4 (pinned): A2's index handler change and A4's index cache-header fix MUST land in the same code change. A hand-built substituted response does not inherit FileResponse's auto etag/last-modified — whatever response type index() ends up returning, `Cache-Control` must be applied to it explicitly, never assumed from defaults.

Acceptance: two dashboards for two different projects open side by side show distinguishable tab titles, each starting with its own folder name; title correct even while data is still loading.

## A3 — People view read-only enhancements (task 5d4e2bd60d17)

Per the task detail (verbatim scope, all read-only):
1. UNREGISTERED WRITERS section: emails appearing in recorded_by/created_by stamps with NO matching person node (casefolded, same `_node_email` semantics as prime's `_distinct_stamped_emails`). Show: email, distinct name(s) seen on stamps, node count, first/last seen. One full node scan is acceptable (same cost class as other dashboard endpoints).
2. Per-person activity drilldown, wiring pinned: "roster-row click" = the EXISTING card click (app.js already calls openDrawer(card.dataset.id) on People cards). The drilldown content renders INSIDE the person-node drawer via type-conditional rendering — not a new surface. The existing "[data-activity] View activity" button (jumps to Activity tab with author filter) is KEPT as-is; it complements the drawer, don't remove or fold it. Drilldown content: node counts by type, last-active timestamp, currently claimed tasks (claimed_by match), open tasks created.
EXPLICITLY OUT: register/edit/deregister affordances of any kind — revisit only with Colton's explicit approval.

Acceptance: on this repo's graph, the unregistered-writers section lists the stamped writer emails that lack person nodes; clicking Colton's roster row shows a coherent drilldown; no POST/PUT/DELETE endpoints added.

## A4 — Rider: cache headers on dashboard assets (task 3f43360014ee, not epic but same files, same pain)

Today /static responses carry only etag+last-modified; browsers heuristically cached 0.18.0 CSS/JS across the 0.27.0 upgrade and produced the mangled-page incident (2026-07-15). Fix so a plugin upgrade + PLAIN reload (no hard refresh) renders the new UI — `Cache-Control: no-cache` (revalidate-always) on index + static, or versioned asset URLs; your choice, note the tradeoff.

Acceptance criterion is the task's: upgrade + plain reload renders new UI. Simulate: serve, load, mutate a static asset, plain-reload, assert new content (a test at the Starlette TestClient level asserting response headers is acceptable).

## Gates (all before handoff back)
- pytest full suite green (baseline 1056 passed/1 skipped), ruff clean, pyright ≤ 39 errors (current local baseline — no NEW errors).
- New tests for: board grouping (epic ancestor resolution incl. deep nesting + no-epic), title injection, unregistered-writers endpoint (registered person excluded, casefold match), drilldown endpoint, cache headers.
- No journal.jsonl commits on the WP branch. No CLAUDE.md edits. No version bump on this branch (bump happens at release boundary, Vince's call).
