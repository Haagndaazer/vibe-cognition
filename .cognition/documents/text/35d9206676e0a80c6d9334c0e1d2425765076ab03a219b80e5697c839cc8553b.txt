Full brief text stored via file_path; key content: WP-EnvFacts-A creates .cognition/people/<slug>.jsonl (committed, one per identity, casefolded-email slug percent-encoded with pinned lowercase hex + sha256 truncation fallback at 40 chars). Registration/profile stay in main journal; ONLY env-fact deltas stream to person files via new fact_set/fact_delete/fact_clear action lines (one line per change — the file IS the audit trail, O(n²) journal growth structurally impossible). Facts hydrate into a per-email registry OUTSIDE the graph (precedent _reference_index), making person-file-without-registration-node a first-class legal state (registered:false) and dissolving cross-file replay ordering. Self-only writes via NO email parameter (impersonation-resistant by construction): cognition_set_env_fact, cognition_delete_env_fact, cognition_clear_env_facts (bulk removal — machine-scoped or all, cleared_keys enumerated). Machine key = casefolded hostname, cap 10 with retryable rejection (never silent LRU). Disclosure field REQUIRED in every write response (interim: agent-relayed only until WP-B's prime listing). Catch-up: dir-mtime gate on people/ scan (peer-review HIGH) then per-file stat gates — O(1) stats on no-change hot path, benchmarked at N=50/100. Hygiene: GIT_HYGIENE_VERSION 5→6, .gitattributes gains people/*.jsonl merge=union (first bump touching the gitattributes writer — must generalize the single-rule check). Flush-protocol topology-guide update lands in the SAME PR (branch-clobber class). Acceptance includes fails-before on: no-op suppression, self-only, slug collisions, registered:false join, merge=union glob, anti-O(n²) linear size, machine-cap rejection; plus fresh-project mkdir, hex-case determinism, bulk-clear semantics, concurrency/union-merge, Windows MAX_PATH. Rejected: facts in person-node metadata (O(n²) confirmed), environment node type, machine-local-only, update_node-shaped person-file lines, LRU eviction, email-param-with-ACL, username scrubbing, per-fact history arrays. Known limits: repo-scoped facts, hostname collisions/CI churn (cap+prune backstop), open reads by design, delta lines persist in git history after delete, orphaned fact files after person-node removal, compaction deferred, disclosure agent-compliance-only until WP-B.