Approved implementation brief for WP-TC16 (task 9f563272da15, epic cedf4a8457e9). Peer-review round 1 REJECT (3 blocking: reports_to vs actual reports_to_email field name — would have made role derivation silently empty; dashboard-changes wording contradicting the required api.py:21 import edit; missing subordinate-section placement) + 5 lesser findings; all applied; revision APPROVED. Contracts: role derivation from ONE person-node scan (manager = someone's reports_to_email casefolds to session email; subordinate = own person node's reports_to_email non-empty; middle manager = both; role-less/unregistered/personalization-off = prime BYTE-IDENTICAL, the strongest fixture). Manager section "## Your Team": one TASK scan bucketed by claimant email; in-progress rows with claimant+age, stale iff now-claimed_at > timedelta(days=prime_stale_claim_days default 7) STRICT (exactly-7d not stale; null claimed_at never stale), blocked rows; prime_rollup_cap default 5 total, stale-first ordering, overflow line. Subordinate section "## Your Manager's Recent Decisions": decisions by manager email, NO HEAD-filter (mirrors _format_decisions exactly — the global model has no supersedes check), prime_manager_decision_limit default 3; own-claims half of the ruling already delivered by Your Open Tasks claimed_by branch (documented mapping). Pinned order: Team Critical → Your Team → Your Manager's Recent Decisions → Your Recent Activity. Helper relocation: _task_claimed_at moves to a light shared module (cognition/task_meta.py or models.py — both verified cycle-free), cognition_tools RE-EXPORTS the name so the old import path keeps working (test_task.py:32-37 unmodified), dashboard/api.py:21 gets the mechanical one-line path edit (authorized in-scope plumbing), prime imports the shared home — prime stays tools-free. Three new knobs in PrimeConfig + Settings with the defaults-equivalence test extended. Terminology: reporting relationship ≠ person.role job title. Known limits: single-manager (ruled), pre-TC4 claims never stale, unregistered claimants invisible to the rollup. Bump HELD — batches with TC14/TC15 as 0.24.0 candidate.