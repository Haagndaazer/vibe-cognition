Scope: WP-TC5 + WP-TC6 — Identity Node Layer. Status: APPROVED (peer-reviewed, 9 changes incorporated). Vorpid implements; Vince gates. One release batch. Epic cedf4a8457e9; task 1c29cff92e20.

FINALIZED RULINGS (Colton): person nodes model HUMANS ONLY (agents stay in teammate-comms); in-place updates with audit trail (not supersession); email is the identity key; agent-origin bool on every write, client-declared, DEFAULT TRUE (via agent), human-origin is the marked assertion; weighting is TC9 (data only here); onboarding is TC7 (tools here must suffice for it).

WP-TC5 DESIGN: New CognitionNodeType.PERSON. Full node-type survival touchpoints: models.py enum; storage/journal replay; PERSON added to _INERT_TYPES (deterministic-edge matcher would otherwise treat it as entity — task/workflow precedent, test in test_deterministic_edges.py); _embed_entity_node wiring; dashboard TYPE_COLORS; cognition_record NODE TYPES docstring (person NOT creatable there — dedicated tools only, like document); hardcoded node_type lists in search/get_history/get_edgeless_nodes docstrings gain "person"; readme.py COGNITION_GUIDE (verified safe — on-demand tool only, not session-injected); SKILL.md + README (test_doc_drift force-fails until updated); SERVER_INSTRUCTIONS explicitly NOT touched (TC7 territory).

Shape: summary "Name — role" (regenerated on update when name/role change); metadata.person = {email (casefolded), name, role, seniority, reports_to_email ("" = top)}; seniority PINNED closed set ordered "owner" > "senior" > "mid" > "junior", module-level constant for TC9 to import; metadata.profile_history append-only [{changed: {field: {from,to}}, at, by(server-resolved)}]; recorded_by as usual. Invariant: one person node per email — re-register returns existing node with "already_registered" signal.

Tools: cognition_register_person(name, role, seniority, reports_to_email?, email?, detail?) — email omitted = server resolves current git identity (self-registration, impersonation-resistant); explicit email = registering someone else, trust-based, audit-trailed; empty resolvable email + no explicit email = clean error. cognition_update_person(email_or_id, fields...) — in-place, one profile_history entry per call, "" clears reports_to, anyone-may-update-anyone stated in docstring. cognition_get_person(email_or_id) + cognition_list_people() roster (may propose folding at gate). reports_to: self rejected; cycles rejected via email-keyed chain walk over person-node scan (casefolded, linear-scan convention — NOT the task-parent edge-walk; dangling email TERMINATES the walk gracefully, never errors); dangling legal, surfaced as reports_to_registered: false. Embedding: like entities; re-embed on update (vector + metadata). NO prime changes; solo prime byte-identical asserted.

WP-TC6 DESIGN: from_agent: bool = True param on cognition_record, cognition_add_task, cognition_store_document, AND register_person/update_person (same batch — person nodes must not be born legacy). Stamped metadata.from_agent. Docstrings: set false ONLY when the human explicitly dictated/authored; when in doubt leave default. Reads: surfaced in search rows, get_node, list_tasks; missing key = absent/unknown, NEVER coerced (TC9's visible legacy fallback owns rendering). No weighting.

BACK-COMPAT: additive only; old journals load. Cross-version traced at review: old-version replay safe (raw string type); live-search catch-up safe (per-node try/except); old-version STARTUP EMBEDDING SYNC unguarded — first person node aborts the sync pass, everything behind it silently never embeds, recurring per restart until upgrade; old-client update_node on person = raw enum error. Actions: regression test that OUR sync has per-node unknown-type tolerance; CHANGELOG/README rollout note (upgrade promptly or local search coverage silently degrades). .cognition/ layout, config, env surface unchanged.

ACCEPTANCE: person tools per design with tests asserting exact profile_history {changed, by} values; one-per-email; self-registration server-resolved; cycle/self rejection; dangling flagged. from_agent stamped everywhere (default true; explicit false honored; both asserted); pre-existing nodes unaffected; missing key = unknown. Solo prime byte-identical both directions. Old journal fixtures load; per-node sync tolerance regression test; rollout note shipped. person searchable (docstrings updated); re-embed on update asserted; _INERT_TYPES + test. README team-semantics section (trust model + known limits); SKILL.md + COGNITION_GUIDE; SERVER_INSTRUCTIONS untouched. Tool-surface self-sufficiency audit (workflow 67751ebc39bd) before gate. Fix+proof same commit; gate = uv run python -m pytest + uv run ruff check . at pinned commit, isolated worktree, import-provenance; version bump both files + uv lock regen; not install-mechanics (flag immediately if that changes).

REJECTED: supersession-versioned people; roster config file; agent names in graph; client-passed caller identity for self-registration; person creation via cognition_record.

KNOWN LIMITS: reports_to/seniority trust-declared, peer-editable with audit; from_agent client-declared, unverifiable; legacy nodes lack from_agent; freshness unenforced (TC7 reduces cold-start).