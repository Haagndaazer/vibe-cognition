Scope: Personalized Session-Start Prime (per-git-user context). Status: APPROVED by Colton 2026-07-07. Vorpid implements; Vince gates.

FINALIZED DECISIONS (Colton, 2026-07-07): 1. Auto-detect default: solo graphs (<=1 distinct non-empty stamped email) get byte-identical output; multi-user graphs personalize automatically; PRIME_PERSONALIZE=auto|on|off env override ships. 2. "Your recent activity" includes episodes, decisions AND discoveries, capped at 5 of each (per-type caps, config knobs). Exceeds the original flat-token recommendation; growth is Colton-approved. Acceptance: measure and report multi-user prime size at gate; solo prime byte-identical. 3. Task-claiming IN scope: cognition_update_task status->in_progress stamps metadata.claimed_by={name,email} from git identity (file-reads only); free-text owner untouched, display-only. "Your Open Tasks" matches created_by.email OR claimed_by.email. 4. The _format_constraints HEAD-filter fix (task 0d7e84d52537) folds into WP-P13n-2.

PROBLEM: hooks/session-start.sh -> python -m vibe_cognition.cognition.prime injects a single global digest. On a multi-user team sharing one graph via git, every section amalgamates everyone's work; a returning user's own open tasks and recent episodes are pushed out by section caps.

GOAL: personalize to the current git user (server-resolved), their tasks and recent activity first; shared knowledge (constraints, workflows, high-severity incidents) stays global.

VERIFIED FOUNDATION: git_identity.resolve_git_identity(repo_path) is file-read-only (NO subprocess — settled v0.12.1 P0 constraint), returns {name, email}, never raises. Task nodes carry server-resolved metadata.created_by={name,email} (trustworthy). All other nodes: author is caller-provided free text (mix of human and agent names) — NOT reliable for filtering; open attribution-honesty task 73f750d8d528. PrimeConfig frozen dataclass + env knobs in config.py is the sanctioned toggle surface. Prime is journal-read-only and light-import and must stay that way (hook timeout; Wedge/Sidecar program).

DESIGN — PHASE 1 (WP-P13n-1): server-stamped provenance. _record_node stamps metadata.recorded_by={name,email} on every node write (same mechanism as _add_task's created_by). author untouched (stays "who dictated the record"); recorded_by is "whose machine/git identity recorded it". Additive metadata; old readers ignore unknown keys; historical nodes lack the key. Also closes the attribution-honesty gap going forward. PLUS claiming: cognition_update_task status->in_progress stamps metadata.claimed_by.

DESIGN — PHASE 2 (WP-P13n-2): prime personalization. Resolve identity once in prime.main(). Multi-user auto-detect: count distinct NON-EMPTY recorded_by.email (fallback created_by.email); <=1 -> today's global digest unchanged (empty-email guard was the peer-review blocker: resolve_git_identity returns email="" when unconfigured; a solo user setting user.email mid-history must not flip to multi-user). When multi-user: Active Constraints global unchanged; Open Tasks split into "Your Open Tasks" (created_by.email OR claimed_by.email == current email ONLY) up to N then "Team critical" (critical/high) up to M; Workflows global; Documents count global; Patterns/Decisions global in v1; Incidents global; NEW "Your recent activity" section: your last episodes/decisions/discoveries (recorded_by.email match), 5 each. MATCHING RULE: email is the ONLY match key. owner and author are display-only, never matched (peer-review should-fix: owner-name fallback both leaks on loose name matches and drops agent-owned tasks). No email resolvable -> global digest. FOLD IN: _format_constraints supersession HEAD-filter (task 0d7e84d52537), mirroring _format_workflows' existing pattern.

REJECTED ALTERNATIVES: semantic relevance-biasing of patterns/decisions (drags embeddings/chromadb into the hook path — violates Wedge/Sidecar; could later go through the sidecar); per-user config file mapping agent names to humans (manual bookkeeping that rots); client-passed identity parameter (reintroduces the trust gap tasks solved by server-resolving); per-user filtering of constraints/incidents (actively harmful — they bind everyone).

KNOWN LIMITS: historical nodes lack recorded_by ("Your recent activity" starts sparse; tasks personalize immediately). recorded_by = "recorded on a machine with your git identity" — agent-recorded nodes on your machine count as yours; manager recording a teammate's relayed fact stamps the manager; conflates on shared machines/CI; acceptable v1, must be in user-facing docs. Same email on two machines = same user (correct). Pre-existing composition hazard: _format_constraints has no HEAD filter (folded fix, decision 4).

WP-P13n-1 ACCEPTANCE: every cognition_record/_record_node node carries metadata.recorded_by (name non-empty, email may be ""); claiming (status->in_progress) stamps claimed_by, re-claiming re-stamps, other transitions untouched; identity failure degrades to {name: getpass-or-unknown, email: ""} never raises never blocks; old journals load unchanged; fix+proof same commit; tests assert specific stamped values. KNOWN-INTENTIONAL: author stays free text (no validation); owner stays free text display-only; created_by unchanged.

WP-P13n-2 ACCEPTANCE: solo graph output byte-identical to current main EXCEPT superseded constraints no longer duplicate (assert both directions); multi-user fixture produces personal sections matched strictly by email, no name matching anywhere; unconfigured identity -> global digest, no personal sections, no crash; superseded constraint (incoming SUPERSEDES edge) never renders, its HEAD does; no new imports in prime.py beyond stdlib/existing (no torch/chromadb); measured multi-user prime token size reported in PR. KNOWN-INTENTIONAL: v0.13.0 trim knob defaults settled — don't rebalance; constraints/workflows/incidents/documents remain global by design; mirror _format_workflows HEAD-filter, divergences come to Vince first.

GATE & RELEASE: pinned gate command, identical invocation, whole-repo: `uv run python -m pytest` and `uv run ruff check .` from repo root. Vince re-runs at pinned commit in isolated worktree with import-provenance check. WP-P13n-1 ships alone (version bump), WP-P13n-2 follows (version bump); Loki pins each; human hands-on check before each release.

PEER REVIEW RECORD: general-purpose subagent (sonnet), 2026-07-07, verified claims against prime.py, cognition_tools.py, git_identity.py, models.py. Incorporated: (blocker) empty-email flips solo to multi-user -> count non-empty emails only; (should-fix) owner-name matching leaks/drops -> email-only matching; (should-fix) superseded-constraints defect composes with "constraints unchanged" -> surfaced + folded per decision 4. Confirmed non-issues: prime perf (in-memory scans, no heavy imports); metadata additive-safety across older plugin versions (dict contents not schema-validated); no structural consumers of prime section headers; empty-graph onboarding path short-circuits before Phase 2 logic.