Scope: WP-TC11 — Dashboard V1 (PM core). Status: APPROVED (peer-reviewed, 6 corrections). Queued: hands to Vorpid when WP-TC5/6 gates. Design source of truth: docs/260714-dashboard-redesign.md + mockup (layout only — doc spec wins). Direction Colton-approved (decision 7f6f6bf22ca9). Task 30fabf12c81b; epic cedf4a8457e9.

IN: nav rail replacing 3-column layout; Overview; Board; shared detail drawer; GET /api/tasks; GET /api/overview (REQUIRED); constellation demoted to lazy Graph tab; header keeps search/banner/refresh/stats.
OUT: V2 (Workflows/Documents/Activity), V3 (person-NODE lookups/People view, seniority badges, live conflict banners — SLOTS only), any new write path (read-only v1), framework migration, CDN assets.

KEY ENGINEERING POINTS (peer-review verified):
- /api/tasks row: {id, summary, status, priority, owner, parent_id, depth, created_by, claimed_by, timestamp, claimed_at, last_transition_at, transitions_count}. claimed_by/transition fields are NOT in _list_tasks's shape — implement the dashboard handler independently against storage.get_nodes_by_type(TASK); do NOT extend the MCP tool contract (no MCP surface in this WP). claimed_at = at of latest →in_progress transition; last_transition_at = transitions[-1].at; node top-level timestamp is creation-only. Claim age + done-7-day-cap derive from these.
- /api/overview REQUIRED: server-computed aggregate — task counts by status, done-this-week, active constraints (severity≠low, HEAD-filtered via the one-line storage predicate `not storage.get_predecessors(node_id, SUPERSEDES)` — do NOT import prime.py: private markdown funcs + CLI deps), needs-attention (stale claims >5d via claimed_at; blocked), recent episodes (5), recent incidents (high+ 14d).
- /api/node/{id} passes metadata through already — NO backend change; drawer renders provenance block (recorded_by/created_by/claimed_by/author with trust-class labeling: server-resolved = normal identity chip; author-only pre-P13n fallback = dashed "unverified"), transition timeline, related nodes grouped by edge type. from_agent badge when key present (TC6 lands first); absent = render nothing. Conflict banner SLOT renders from a `conflicts` field if present — server population is Wave-2 TC2; inert in V1.
- Identity chips (V1) ≠ person-node chips (V3): V1 renders identity strings from node metadata only, no person-node fetch/navigation.
- Lazy Graph tab: the BUILD is lazy, and the poll must be decoupled — today init() unconditionally loadGraph()→buildCy() and the 30s interval rebuilds a NEW cytoscape instance each cycle. Required: stats/overview polling always; graph JSON fetch as needed by feeds; cytoscape() construction ONLY while Graph tab active/on first activation.
- Auth: token middleware already gates /api/*; missing/invalid token returns 403 (established dashboard status — NOT 401), match siblings in tests.
- Threadpool risk task 4163f54f2848: assess; fix here if small, else written accept-rationale at gate. Token hygiene (shipped): don't regress no-URL+token-logging.

ACCEPTANCE: V1 views work on real graph (manual /vibe-dashboard smoke) + empty-graph fixture; /api/tasks + /api/overview contract tests (403 no-token; shape; done-cap from last_transition_at; claimed_at vs claimed fixture); drawer transition timeline + trust-class fallback asserted (author-only fixture); lazy-BUILD verified incl. poll not constructing Cytoscape on other views; read-only invariant (no new mutating endpoints); D-3 (d7071a4377ee) behavior confirmed and closes at gate; README dashboard section updated; vanilla JS + vendored only; status colors labeled never color-alone. Gate: pytest + ruff at pinned commit, isolated worktree; version bump both files + uv lock after code sign-off; not install-mechanics.

KNOWN LIMITS: dashboard is identity-blind (why read-only); done-this-week is transition-timestamp based; single-repo view.