Approved implementation brief for WP-TC4 (task f75cf9fa2b72, epic cedf4a8457e9). Three peer-review rounds (rev 1 REJECT: note-guard interaction + wrong test enumeration; rev 2 REJECT: bare same-status poke dies at the no-updatable-fields gate :1673-1679, and the same-status takeover had no mutation path; rev 3 APPROVE-WITH-CORRECTIONS, applied). Contracts: structured claim_warning key (kinds claim_collision / takeover_note_required / reopen; claimant + claimed_at + optional assigned_to info + message; absent-key discipline). Live claim = claimed_by present AND status in {in_progress, blocked}; identity = casefolded email equality, either-side-empty disengages everything. 2a transition-based takeover (blocked→in_progress over live foreign claim): note required, retryable rejection before mutation. 2b same-status takeover with note: NEW mutation branch parallel to the :1605 body; identity hoisted before the :1593 note guard; guard condition gains AND NOT same_status_takeover_shape; snapshot prior claimant before restamp. 2c foreign poke without note: no seizure, warns "you did NOT take it over"; bare poke succeeds via a no-updatable-fields gate carve-out keyed on the stashed warning, falling through to normal reembed+assembly. Reopen: warning on foreign done/cancelled→open naming closer + closed-at; suppressed when no closing transitions entry. _task_claimed_at extracted from dashboard/api.py:314-321 into the tools layer, single shared implementation. Test changes enumerated: test_task.py:418 gains note + sibling rejection test; :479 stays green unmodified. Never-blocks vs note-required interpretation recorded as a decision node, flagged for Colton's async review.