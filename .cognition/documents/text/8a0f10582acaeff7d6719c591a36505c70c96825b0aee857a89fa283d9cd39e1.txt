# WP Brief — Train B: Provenance & tool-surface small fixes (store_document stamp + seniority docstring + TC2 conflict naming)

Author: Vince. For: Vorpid. Epic: cedf4a8457e9. Tasks: 2858ae93bf17, 9c7094bbbe0e, 888a21f729dd.
Branch: one WP branch (suggest `wp-provenance-smalls`). Three independent small fixes, one branch, one review.

## B1 — store_document stamps recorded_by (task 2858ae93bf17)

`_store_document`'s metadata dict (cognition_tools.py ~1160-1168) never stamps recorded_by, unlike _record_node/_add_task/_register_person which all stamp `resolve_git_identity` verbatim at write time. Fix: stamp `recorded_by` the same way (verbatim, same shape as the other write paths). Docstring note. Also check whether `list_documents`'s row dict (dashboard/api.py — its own inline dict, a SEPARATE code path from `_entity_row`) should add a display-only `recorded_by` field (tiny if so).

Sequencing consequence to note in the WP: this gives document nodes a live stamp shape, which is the prerequisite for backfill v2's document scope (Colton confirmed documents stay OUT of backfill v1 until this ships).

Acceptance: a freshly stored document's metadata carries recorded_by {name, email} matching git config; row-shape test; docs unchanged elsewhere.

## B2 — register_person/update_person docstrings present the seniority closed set (task 9c7094bbbe0e, Colton-dictated from live experience)

During his own registration Colton said "owner, senior" and the agent had to positionally guess — the closed set (owner|senior|mid|junior) was never shown to him, and he landed on the wrong tier. Fix: the docstrings of cognition_register_person AND cognition_update_person must explicitly instruct the calling assistant to ENUMERATE the four seniority tiers to the human and ask which applies, rather than silently mapping free text; plus one line distinguishing role (free text) from seniority (closed set).

Acceptance: fresh-eyes read of the docstring alone leads an LLM to offer the options (the recurring tool-surface self-sufficiency audit is the standard); no behavior change to the tools themselves. Automated-test substitute (acceptable): a docstring-content assertion checking the four tier words appear AND an explicit instruction to ask the human which applies is present, in both tools' docstrings.

## B3 — Conflict warnings NAME the counterparty (task 888a21f729dd, TC2 — the epic's last read-surface gap)

Verified live today (2026-07-16): search hits with contradicts edges correctly carry `conflicted: true` (WP-SearchFlags), but the flag is a bare boolean. TC2's acceptance requires the warning to NAME the conflicting decision, its author, and the conflict. Fix, read-side only: when a hit is conflicted, include the counterparty details — `conflicted_with: [{id, summary, author, reason}]` (author from recorded_by stamp when present, else free-text author; `reason` from the edge, null when the edge carries none — never fabricate one). Same HEAD/live-edge semantics as the flag; keep the boolean for compatibility.

IMPLEMENTATION APPROACH, PINNED (peer-review blocking catch): `conflict_flags()` (cognition/queries.py:153-206) returns a strict 2-tuple and is unpacked at TWO call sites — dashboard/api.py:381 and cognition_tools.py:743 (search formatting). Do NOT widen its return shape. Add a SIBLING helper (reusing storage.get_predecessors/get_successors(node_id, CONTRADICTS), which already return (id, edge_data) pairs — see the supersedes tie-break at queries.py:196-204) called ONLY from the search formatting loop. The dashboard's conflicted-⚠ behavior and its tests must be re-verified unchanged.

HARD BOUNDARIES (rulings, do not cross): read-side advisory ONLY — no write-time advisory (Colton ruled that permanently out 2026-07-16, decision 1583c5391354), no blocking, no acknowledgment mechanics (M3). Penalty-only weighting doctrine untouched — this adds information to a hit, never hides/drops/re-ranks one.

Acceptance (task's own): a search whose results include a conflicted decision carries an explicit warning naming the decision, its author, and the conflict. Verify live on this repo's two real contradicts pairs (f09e770da046↔d79cd9a93a02, 3c16d91417cb↔03ae7b2cd063) plus unit tests (conflicted node, clean node, dangling edge target). Prime digest: NOT in scope — search results only (prime already has its own conflict surfacing story; expanding it is a separate decision).

## Gates
- pytest green (baseline 1056/1), ruff clean, pyright ≤ 39 (no new errors).
- New tests per fix as above. Docstring changes ride the tool-surface audit workflow — run its checklist for the three touched tools.
- No journal.jsonl on the WP branch; no CLAUDE.md edits; no version bump on-branch.
- Closing note per task id in the PR/WP notes so Vince can close 2858ae93bf17, 9c7094bbbe0e, 888a21f729dd on merge (TC2 closure also updates the epic ledger).