# Fable Audit — Stage 3: Consumer Audit — 2026-07-01

Four Sonnet auditors as newcomers: literal README walkthrough; agent for whom tool docstrings ARE the docs; team adoption arcs (teammate #2, topology, attribution, cross-project); skills/CLI/error UX + payoff reachability. Yardstick: confirmed intent (install → within a session or two: recording, retrieval, and after /vibe-curate a browsable linked graph; multi-agent/multi-human first-class). Full detail in docs/260701-fable-audit-consumer.md.

FINDINGS (condensed):

1. [high/blindspot] Joining teammate told "ready" while search silently incomplete: embedding_ready.set() fires (server.py:206) BEFORE the backfill sync loop starts (:215); no "syncing" status exists in get_status (ready/loading/error only); README's only guidance covers model load, not corpus sync; the cognition_reload row's "auto-converges with concurrent sessions" overstates against high-severity drift discoveries (4b99fa9f44d5, 55b6740e42f8); no teammate-join walkthrough or timing expectations anywhere (onboarding fires only for EMPTY graphs).
2. [high/gap] Shared-checkout protocol unshipped: the maintainers' own topology (worktree flush, no journal commits on WP branches, destructive-op ban — constraints 1f39e60c6d83/4ed473ba9c75, born of incidents 5d63a548783d/59416463f1e3) exists only in the private graph + personal team-rules files; README mentions "shared-checkout" once undefined; cognition_readme teaches separate-clones only. Neither topology is both exercised and shipped (Stage 2 showed the shipped one is untested).
3. [high/bug+gap] README accuracy: Troubleshooting's "Only one Vibe Cognition instance can run per project" is stale-false (initial-commit vintage; contradicts rust-lock/multi-client discoveries and the maintainers' daily mode — real caveat is startup-hydration divergence); tool tables list 25 of 29 (missing cognition_readme + all three cross-project tools); cross-project feature has zero README presence; troubleshooting omits the incident-backed venv self-heal ("close ALL sessions", 3432e00e483d) and Windows orphan pileup; Quick Start hides the uv-sync wait and has no success signals; EMBEDDING_REVISION absent from config table.
4. [high/gap] Edge semantics only in skills/vibe-curate/edge-analyzer.md (loaded solely by the curate subagent); cognition_add_edge docstring gives a bare enum while cognition_record's docstring invites direct hand-linking — agents guess led_to vs relates_to vs resolved_by.
5. [high/unclear] cognition_record over-promises: "nodes link via deterministic part_of matching" reads general but the truth table (storage.py:662-708) never links entity↔entity — mid-episode decision clusters sit edgeless until curation; NODE TYPES omits document entirely (spec content stuffed into discovery loses sidecar/freshness/dedup) and reveals task only via rejection error.
6. [medium/broken-assumption] Curated-graph payoff is agent-only: /vibe-curate reports counts, never WHICH nodes connected or why; the only human-browsable surface is the (secondary, separately-invoked) dashboard — the success criterion's "browsable linked graph" is unreachable for a human who never opens it.
7. [medium/blindspot] Mandatory auto-curate fans out Haiku subagents (uncapped batches) with zero cost/scope warning before or during — only a retrospective report; cadence guidance unquantified.
8. [medium/gap] SERVER_INSTRUCTIONS (the only pushed contract, "three standing practices") never mention cognition_list_tasks or cognition_get_workflow though both docstrings declare themselves mandatory pre-work gates.
9. [medium/gap] cognition_search returns node_type; every other read tool returns type — undocumented inconsistency, silent misclassification.
10. [medium/broken-assumption] Attribution trust asymmetry: task creator is git-resolved server-side (cannot spoof) but cognition_record author is arbitrary client text (agent personas in practice); README never mentions author/attribution at all.
11. [medium/bug] vibe-cognition-prime/-backfill silently swallow --help and fully execute (no argparse at all; verified live); dashboard CLI's argparse is fine.

Walkthrough trace: no install success signal → hidden two-stage first-session wait → no "is it working" moment → under-counted tool tables → stale/missing troubleshooting → teammate #2 has no join path and a lying "ready".

THEMES: (1) First impressions are unmanaged — silence or overstatement at every trust-forming moment (install, first wait, "ready", stats-only payoff). (2) The maintainers' knowledge is not the consumers' knowledge — the knowledge-transfer product doesn't transfer its own lessons (protocol, caveats, troubleshooting, features all in graph/code but not shipped docs). (3) The agent-facing contract has authority gaps — pushed instructions incomplete, semantics locked in a subskill, record over-promises. (4) Trust metadata inconsistent — verified vs spoofable authorship, undocumented.

10 tasks filed under the fable-audit epic (74740daa02fa); see the doc's checklist.