Approved Gate C2 brief (gate task 85b2b4de8aee, epic cedf4a8457e9), peer-reviewed APPROVE-WITH-CORRECTIONS with 3 HIGH pass-bar fixes applied. Part A (Vince runs, writes no code): A1 weighted-search latency sweeping BOTH total node count (1k/5k/10k) AND roster (0/10/100) — the real cost driver is get_nodes_by_type's whole-graph scan (O(total_nodes), not O(roster)); overhead bar ≤10ms p95 delta vs roster-0 at same node count, linear-in-nodes growth disclosed, NO cross-mode comparison (single-project times _search_cognition directly; project="*" must go through ctx dispatch — each judged against its own control). A2 prime: (i) like-for-like re-run of C1's exact B3 scenario at the C2 pin vs episode 3a1d1096b57a's ~11ms medians; (ii) new-topology absolute budget — 1/5/25 users, 3-level reports_to chain, manager with 10 reports, p95 ≤ 250ms + every section cap bounds output (prime_rollup_cap=5 per TC16 brief doc:01acff533612). A3 replay comparison via bench_wave1 --journal-dir vs C1 figures. A4 real-graph prime render sanity. A5 chromadb flake tally (escalate at ≥3 same-test flakes across epic gates). Part B harness (Vorpid WP, after TC16 gates — single-implementer pipeline makes earlier splitting pointless): extend bench_wave1.py or sibling — --persons/--nodes knobs, stamped/from_agent/claimed_by+backdated-stale fixture population, search + prime + multi-project benchmark modes, fixed-vector embedder injection, scan-count via monkeypatched _person_seniority_map counting wrapper (flagged fragility, scripts-only), JSON tail, seeded, no product code, no release (C1-harness precedent ed5d53a). Remediation substitution flagged for Colton: "blocks next wave" has no next wave after Wave 3 — C2 failure blocks the epic-close gates (B-final/D).