Approved implementation brief for WP-TC9 (task f746c5f9361e, epic cedf4a8457e9), peer-reviewed APPROVE-WITH-CORRECTIONS with all 8 findings applied. Core: penalty-only multiplicative weighting (multiplier ≤ 1.0 from a 1.0 baseline) makes the ruled invariants structural — constraints/incidents pinned 1.0 (reuse _NEVER_EXCLUDED_SEARCH_TYPES) can only rise; unverifiable = neutral 1.0; nothing filtered (total_found/exhaustive untouched). Two axes: seniority from person nodes (monotone down SENIORITY_LEVELS, small spread) and agent-origin penalty STRICTLY stronger than the whole seniority spread (human always outweighs agent, verified well-defined incl. unregistered-neutral vs agent-of-owner). Visibility: raw score never mutated; every hit ALWAYS carries weight {multiplier, seniority, from_agent, basis} + weighted_score. Basis vocabulary mirrors the dashboard's EXACT unverified boundary: human:<seniority> / human:unregistered (stamped, no person node — NOT unverified) / agent / exempt:<type> / unverified (no stamp at all). Where: inside _format_search_results (sorts uncapped list by weighted_score so the [:limit] slice takes weighted top); THE safety-critical edit is the multi-project outer merge at cognition_tools.py:3254 switching to weighted_score (else weighting silently no-ops for project="*"). Person resolution: ONE get_nodes_by_type(PERSON) scan per top-level call building an email→seniority map, threaded into the dedupe lambda from _search_with_embedding's enclosing scope (NOT a local in _format_search_results — resets per widening round); cache-inputs-vs-accumulate-outputs distinction from TC10 documented. get_workflow inherits weighting deliberately (can change WHICH workflow matches on multi-topic graphs) — own fixture + unconditional docstring note. Scope: cognition_search only (not get_history/list_tasks/prime/dashboard). Fallback fixtures a-d incl. the compound post-P13n/pre-TC6 unrostered case. Perf fixture forces a widening round and asserts the person scan count stays 1. Version 0.23.0 batching TC4+TC9; TC9 carries the bump.