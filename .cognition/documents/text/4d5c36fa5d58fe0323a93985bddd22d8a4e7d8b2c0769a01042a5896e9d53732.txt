# WP Brief — Train C: Legacy identity backfill v1 (task 962ab7b442d5)

Author: Vince. For: Vorpid. Epic: cedf4a8457e9. Design: APPROVED rev 2 stored as doc:76a545c246ef (node 4464db1cdc2d; supersedes rev 1 doc:e134110987c8). Rulings decision node: 833e9f67de4d. Do not relitigate any resolved question.
Branch: one WP branch (suggest `wp-backfill-v1`). Largest train — read the full design doc first; this brief is the delta contract, the doc is the spec.

## The one design change since peer review (CRITICAL — reworks the mapping sources)

Colton ruled (Q1): MANUAL CONFIRMATION FOR EVERY NAME — nothing auto-stamps, not even exact roster hits. Sources 1 (roster) and 2 (git blame) become SUGGESTION GENERATORS ONLY:

- Dry-run (default) scans, produces the report AND emits a skeleton map file where every author name is a row: pre-filled suggested email where a source found one (annotated with its source: roster / git-history), blank where none did, "(no author)" row included.
- Q6 ruling: where blame shows email drift for one name (e.g. Colton's acryliccode.com vs studiomonsoon.net), the pre-filled suggestion is the MOST RECENT email, with a drift annotation listing all observed emails. The email-consistency DETECTION from peer-review B1 stays — it now powers the suggestion+annotation instead of a hard fall-through.
- `--apply` requires a confirmed map file (`--map-file`); it stamps ONLY names present in that file. `--map "Name=email"` (repeatable) still supported for one-offs. No map file, no writes.
- Q8: agent-persona names ("Vince", "Vorpid", …) appear as ordinary unfilled rows — owner decides per graph, NO default suggestion.

Two honesty notes on this section: (1) the design doc's REJECTED ALTERNATIVES list predates the Q6 resolution and still lists "most-recent-email pick" as rejected — Q6's ruling controls where they conflict (it reintroduces most-recent as a SUGGESTION, never a write). (2) The confirm-everything + most-recent-suggestion MECHANISM is Vince's synthesis of Colton's Q1+Q6 rulings (flagged as such in decision 833e9f67de4d). The individual rulings are settled — do not relitigate them — but if an edge case during implementation suggests the combined mechanism doesn't hold together as described, STOP and flag it to Vince rather than silently reinterpreting.

## Everything else per the approved design (unchanged)

- Marker: keys inside the stamp dict — `"backfilled": true`, `"backfill_source": "roster|git-history|manual"`. (With confirm-everything, note in the doc/WP whether source records where the SUGGESTION came from vs "manual" for hand-entered — recommend: record the suggestion source when the user kept it, "manual" when they changed it or filled a blank; if the map file can't distinguish kept-vs-edited, record "manual" for all applied stamps and keep suggestion provenance in the skeleton only. Your call, note it.)
- Scope: entity nodes → recorded_by, task nodes → created_by. PERSON nodes OUT. DOCUMENT nodes OUT until Train B's B1 ships (then still v2, not this WP). Claims/transitions PERMANENTLY out. Dashboard visual distinction for backfilled stamps: DROPPED (no follow-up).
- Skip predicate mirrors _node_email: "already stamped" iff stamp dict has NON-EMPTY email; legacy empty-email stamps are eligible (H2).
- `--recompute-backfilled`: may overwrite ONLY marker-carrying stamps; server-resolved stamps untouchable under every flag.
- Write mechanics: per-node read → deep-copy metadata → inject → storage.update_node (journal-FIRST append, no rewrite); re-read immediately before write; journal-mtime watch with abort-on-change; concurrency residual documented as ACCEPTED, no CAS primitive.
- Report: mapping table (name → suggested email → node count → source), unmapped-as-main-event UX, exact journal-line count, AUTO-FLIP forecast over BOTH _should_personalize arms (post-backfill distinct stamped emails AND current registered-person count — don't misattribute an existing flip).
- Delivery: `python -m vibe_cognition.backfill_identity <project-path> [--map ...] [--map-file ...] [--apply] [--recompute-backfilled]`, dry-run default, scoped to the one target project. NOT an MCP tool.
- Ships with version bump + CHANGELOG (user-facing console command) — bump lands at the release boundary, coordinate with Vince, not on-branch unless told.

## Test plan (design doc's sketch, updated for confirm-everything; pin these)

- Skeleton emission: roster suggestion annotated, blame suggestion annotated, drift row = most-recent email + all-emails annotation, agent personas + "(no author)" rows blank, no writes in dry-run (journal byte-identical).
- Apply: stamps only map-file names; N update_node events exactly; replay reproduces; idempotent second run no-op; empty-email stamp eligible; non-empty skipped; --recompute-backfilled touches only marker-carrying stamps.
- Bulk-commit exclusion scale-invariant (early-small-journal fixture); flusher-attribution guard (name-agreement gate) still applies to SUGGESTIONS (a blame suggestion is only offered when blame name matches node author — suggesting the flusher's email for an agent-authored node is the same bug at suggestion level).
- End-to-end: legacy author gains Your Activity in personalized prime post-backfill; auto-flip forecast correct in all four quadrants.
- Fixture corpus: snapshot of this repo's own pre-July-7 journal shape (agent personas included).

## Gates
- pytest green (baseline 1056/1 + the new suite), ruff clean, pyright ≤ 39 (no new errors).
- Dry-run demonstrated on THIS repo's real graph in the WP notes (report + skeleton excerpt) — but NO --apply on the production graph in the WP; the real apply is a Colton-driven moment after merge.
- No journal.jsonl on the WP branch; no CLAUDE.md edits.