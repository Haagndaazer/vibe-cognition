# Scope: Gate C1 — Wave 1 Stability Audit (identity layer)

Status: APPROVED (peer-reviewed 2026-07-15, 5 precision corrections: A1 set-arithmetic invariant; harness --journal-dir mode resolves the no-code tension; B4 seam split with conftest fixtures named; B5 concrete catch-up trigger + in-process sufficiency; O(total-nodes) language). Runs AFTER WP-TC8 gates (Wave 1 complete: TC5/6 v0.19.0, TC11 v0.20.0, TC7+TC8 v0.21.0). Split per battle plan: Vorpid builds the measurement harness under this brief (manager writes no code); Vince runs the audit checklist, gates the results, records the C1 report. REMEDIATION CLAUSE (battle plan §3): a C1 failure blocks Wave 2 feature WPs — it becomes a fix WP or an explicit accepted-risk record before TC1 starts. Epic cedf4a8457e9; task 85b2b4de8aee.

## WHAT C1 AUDITS (battle-plan scope, made concrete)

C1 is a GATE, not a WP factory: most items are verification of things the per-WP gates already argued; the harness exists to turn two claims ("cheap" and "converges") into numbers.

### Part A — Vince-run checklist (no code)

A1. **Journal compatibility, real data:** replay THIS repo's production journal (500+ nodes incl. real person nodes, from_agent stamps, assignment metadata once TC8 lands) on a fresh CognitionStorage at the Wave-1-final commit, zero replay errors in logs. **Invariant (peer-review corrected — NOT raw line counts):** distinct-SET arithmetic — expected_nodes = {ids in add_node lines} − {ids in remove_node lines}; expected_edges = {(from,to,type) triples in add_edge lines} − {removed triples}; compare against the replayed graph's get_statistics(). Raw line counts only pass today by luck: add_edge journal lines are not deduped at the tool layer while the graph edge-add is idempotent by triple, and update_node lines never affect counts — set arithmetic stays correct if duplicates ever appear. **No-code resolution:** Vince runs the Part B harness's replay routine pointed at the real journal dir (harness ships a `--journal-dir` mode for exactly this) — the manager runs a script, writes none.
A2. **Additive-only metadata review:** diff-review confirmation (from the four gate reviews) that every Wave 1 metadata key is NEW (person, profile_history, from_agent, assigned_to, assignments) and no existing key changed shape or meaning. Desk check against the shipped diffs, recorded with commit refs.
A3. **Cross-version story recorded:** CHANGELOG upgrade-promptly note shipped (v0.19.0) covering the old-version embedding-sync degradation; nothing in TC7/TC8 adds a NEW cross-version hazard class. The "old replay stores unknown metadata keys inertly" claim is ALREADY code-verified at peer review: _replay_entry's add_node branch raw-passes metadata (storage.py:1150) and update_node blind-assigns keys (storage.py:1206-1213); replay never constructs the Pydantic model at all. Cite those lines in the report.
A4. **Flake review:** the suite ran ≥6 full times across Wave 1 gates (Vince + Vorpid runs). Report any nonreproducible failure/flake observed, chromadb or otherwise. (Today's count so far: zero — confirm still true at C1 time.)
A5. **Onboarding-path behavior:** one end-to-end manual pass in a scratch repo at the final commit: unconfigured email → no notice; configured unregistered → notice; register via the notice's instructions → next prime silent + person node correct (from_agent=false, profile_history empty, recorded_by stamped); decline path → next prime silent, file gitignored (hygiene v3 re-fire observed on a pre-v3 scratch project).

### Part B — Vorpid-built harness (small, one commit, NOT a release)

A benchmark script `scripts/bench_wave1.py` (stdlib + project imports only; NOT shipped in the plugin surface, NOT a test — a runnable report generator) that on demand builds a synthetic graph and prints a machine-readable report:

B1. **Synthetic scale fixture:** ~1,000 mixed nodes (realistic type distribution), 10 person nodes, 100 tasks (some assigned once TC8 lands), 20 people-referencing metadata stamps — deterministic seed data, tmp dir, no embeddings backend required for the replay/prime measurements (embedding-dependent counts are mocked/counted, not timed).
B2. **Replay time:** journal replay wall time at 1k nodes (report; soft threshold < 2s on the dev machine — a regression indicator, not an SLA).
B3. **Prime cost decomposition:** generate_prime wall time on the 1k graph, and the onboarding-detection share measured by running with prime_onboard on vs off (10-run median; verified clean isolation — the flag gates exactly one early return, prime.py:419). PASS: detection share < 10% of total prime time (it's one O(total-nodes) scan filtered to persons — get_nodes_by_type iterates the whole graph — plus one small file read; if it's more, something is wrong).
B4. **Embeds-per-operation counts** (counting mock as the embedding generator): register_person == 1; update_person == 1; add_task (with assignment) == 1; update_task assignment change == 1 (rides the single update path, no double re-embed). PASS: exact counts. **Seam guidance (peer-review): two different seams** — _update_task is a testable core taking explicit storage/embedding_storage/generator/embeddings_ready params (pass the counter directly); _add_task/_register_person/_update_person take ctx and read the lifespan dict — REUSE tests/conftest.py's build_lc (lines 90-134) + make_ctx (137-152) fixture pattern, and note _embeddings_ready requires embedding_ready to be a SET threading.Event with embedding_error falsy, else the embed call silently skips and the counter reads a false zero. build_lc instantiates a real ChromaDBStorage, so B4/B5 touch chromadb (existing hard dep — fine); B1's "no embeddings backend required" is scoped to B2/B3 only.
B5. **Cross-instance convergence spot-check:** two IN-PROCESS CognitionStorage instances on one journal dir (peer-review: the catch-up mechanism is filesystem stat+read, storage.py:953-971 — real multiprocessing adds nothing; do not reach for it). The concrete trigger: any public storage method on B (anything under _synced(), e.g. B.get_node(...)) synchronously runs _catch_up() (storage.py:177-193). Flow: A registers a person + assigns a task → B.get_node sees both with field-exact metadata → B updates the person → A's next public call converges likewise. PASS: field-exact both directions. (Pins the NEW metadata shapes through the existing generic convergence machinery.)

Harness acceptance: script runs clean on the Wave-1-final commit; supports `--journal-dir` (point at a real .cognition for A1) alongside the synthetic mode; report output is copy-pasteable into the C1 record; ruff clean; no production code changes; no new deps. Vince gate on the harness = review + run it, not the full release train (no version bump — scripts/ is outside the wheel's packages list, pyproject.toml:38-39; note the repo checkout still physically carries it, it's just not wired into the plugin surface). DO NOT run the harness before TC8 is merged — B1's assigned-task fixtures depend on TC8's fields.

## OUTPUT

A C1 report recorded in the graph (episode or discovery + refs): checklist results A1–A5, harness numbers B2–B5, verdict PASS / PASS-with-notes / FAIL(→fix WP). Wave 2 briefs do not go to Vorpid before the verdict is recorded.

## NON-GOALS

No weighting/latency-under-weighting measurements (that's C2, after Wave 3 — weighting doesn't exist yet). No dashboard load testing (read-only V1, sync handlers, accepted at TC11 gate). No chromadb soak tests (flake WATCH is a review of observed runs, not a new rig).
