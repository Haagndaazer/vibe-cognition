# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv/
venv/
ENV/

# uv
.uv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Type checking
.mypy_cache/
.pyright/

# OS
.DS_Store
Thumbs.db

# Project specific
*.log
# The MCP server is declared by the plugin (plugin.json), not via a per-project
# .mcp.json. Ignore any stale/local one so it never gets re-tracked.
.mcp.json

# Claude
.claude/

# Local embeddings storage
.embeddings/

# Cognition graph: the journal is committed (shared via git); the ChromaDB
# vector store is per-machine and regenerable, so it stays out.
.cognition/chromadb/