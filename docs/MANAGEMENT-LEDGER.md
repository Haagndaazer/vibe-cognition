# Management Ledger — RELOCATED (this is a pointer)

The cross-project manager ledger's single source of truth is now **global**, not per-repo
(Colton's call, 2026-06-13 — rule 21 turned on ourselves: a per-repo copy is a drift
snapshot by construction and can't be canonical):

- **`~/.claude/team-rules/MANAGEMENT-LEDGER.md`** — the ratified rules (1–23 + margins,
  incl. the (11) retain/reclaim-symmetry margin), cross-project, stable numbering
  (append / refine-in-place / never-renumber).
- **`~/.claude/team-rules/rules-managers.md`** — the manager operating manual, loaded at
  session start via CLAUDE.md. Holds the role, the gate-is-a-hold creed, WP design, the
  gate, the **vibe-cognition journal-flush protocol** (§5, topology-gated), the human
  gate, and escalation.

Model: lessons are **proposed and traded** in the `#manager-lessons` channel (the canonical
trade record); they're **ratified** by writing INTO the global ledger — never a private copy.
Implementers read the global file directly by its machine path and cite rules by number.

This repo's former full ledger snapshot is retired to this pointer to prevent drift; its
history remains in git and in the channel.
